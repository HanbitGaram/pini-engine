<?xml version="1.0" encoding="utf-8"?>
<!--
  [현대화 — HANDOVER.md §6 Phase 4-7]
  원본은 minSdk 9 / targetSdk 미지정이었고, 광고 SDK 4종·Fabric·OBB 확장파일·LVL licensing
  액티비티와 권한이 모두 선언되어 있었다. 1차 릴리즈 범위에서 전부 제거했다.
  package 속성은 AGP 8 부터 build.gradle 의 namespace 로 대체되어 여기서는 쓰지 않는다.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 로컬 예약 알림용. API 33+ 는 런타임 권한도 필요하다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      제거한 권한:
        WRITE_EXTERNAL_STORAGE / MOUNT_UNMOUNT_FILESYSTEMS  (OBB 확장파일용, scoped storage 이후 무의미)
        com.android.vending.BILLING / CHECK_LICENSE          (AIDL 결제 / LVL licensing 제거)
        CHANGE_NETWORK_STATE / CHANGE_WIFI_STATE             (광고 SDK 가 요구하던 것)
    -->

    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/icon"
        android:allowBackup="true">

        <meta-data android:name="android.app.lib_name" android:value="cocos2dlua" />

        <activity
            android:name="org.cocos2dx.lua.AppActivity"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|uiMode|smallestScreenSize"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- API 31 부터 intent-filter 를 가진 컴포넌트는 exported 를 명시해야 한다.
             이 리시버는 앱이 스스로 건 알람만 받으므로 exported=false 가 맞다. -->
        <receiver
            android:name="org.cocos2dx.lua.AlarmReceive"
            android:label="alarmrecever"
            android:exported="false">
            <intent-filter>
                <action android:name="com.piniengine.alram" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
